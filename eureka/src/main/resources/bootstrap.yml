spring:
  application:
    name: Eureka-Server
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
  zipkin:
    base-url: http://localhost:9411/
  sleuth:
    sampler:
      probability: 1